[project]
name = "crypto-radar"
version = "1.0.0"
description = "Binance perpetual futures scanner with a web dashboard"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "pandas>=2.0",
    "numpy>=1.24",
    "scikit-learn>=1.3",
    "joblib>=1.3",
    "pyarrow>=14.0",
]
